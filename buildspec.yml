version: 0.2

phases:
  install:
    runtime-versions:
      python: 3.11
  build:
    commands:
      - zip lambda_function_payload.zip lambda_function.py

artifacts:
  files:
    - lambda_function_payload.zip

